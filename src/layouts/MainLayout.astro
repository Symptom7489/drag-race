---
import '../styles/global.css';
const { title = "Fantasy Drag Race" } = Astro.props;

// Check if user is logged in via cookie - if not, hide Dashboard and Draft links
const userId = Astro.cookies.get("userId")?.value;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body class="bg-slate-50 text-slate-900 font-sans min-h-screen flex flex-col">
<nav class="bg-pink-600 text-white shadow-lg p-4">
  <div class="container mx-auto flex justify-between items-center">
    <a href="/" class="text-2xl font-black tracking-tighter hover:text-pink-100 transition-colors">
      👑 S18 FANTASY
    </a>

    <ul class="flex items-center space-x-6 list-none m-0 p-0">
      {userId ? (
        <>
          <li>
            <a href="/dashboard" class="font-bold hover:text-pink-200 transition-colors">Dashboard</a>
          </li>
          <li>
            <a href="/draft" class="font-bold hover:text-pink-200 transition-colors">Draft</a>
          </li>
                    <li>
          <a href="/leaderboard" class="font-bold hover:text-pink-200 transition-colors">Leaderboard</a>
        </li>
        </>
          <li>
            <a href="/logout" class="bg-pink-800 px-4 py-2 rounded-lg font-bold hover:bg-pink-900 transition-all shadow-md">
              Logout
            </a>
          </li>

      ) : (
        <li>
          <a href="/login" class="font-bold hover:text-pink-200 transition-colors">Login</a>
        </li>
      )}
    </ul>
  </div>
</nav>

    <main class="container mx-auto p-6 flex-grow">
      <slot />
    </main>

    <footer class="bg-slate-800 text-slate-400 p-8 text-center">
      <p>© 2026 Drag Race Fantasy League • Season 18</p>
    </footer>
  </body>
</html>